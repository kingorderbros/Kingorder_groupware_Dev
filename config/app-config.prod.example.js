/** 운영(prod) 설정 본보기 — 실제 값은 GitHub Secrets(SUPABASE_URL_PROD · SUPABASE_ANON_KEY_PROD)에서 배포 때 채워집니다. */
window.KOB_CONFIG = {
    env: 'prod',
    supabaseUrl: '__SUPABASE_URL__',
    supabaseAnonKey: '__SUPABASE_ANON_KEY__',
    apiBase: ''
};
