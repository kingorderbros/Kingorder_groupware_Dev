# 참고용 — 이 앱은 .env 를 직접 읽지 않습니다.
#   브라우저 설정  → config/app-config.js (anon 키만)
#   서버(functions) → .dev.vars (로컬) / Cloudflare Pages 환경변수 (배포)  → .dev.vars.example 참고
#   배포 자동화    → GitHub Secrets (.github/workflows/deploy-pages.yml 머리 주석 참고)
